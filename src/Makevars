#RCUDA.so: device.o
NVCC=nvcc
CUDA_DIR=/usr/local/cuda
CUDA_INC=$(CUDA_DIR)/include
PKG_CPPFLAGS=-I$(CUDA_INC) -DDEBUG_R_FINALIZERS
PKG_LIBS=-L$(CUDA_DIR)/lib -lcuda -lcudart

%.o: %.cu
	$(NVCC) -c $< $(CUDA_INC) -I$(R_HOME)/include


